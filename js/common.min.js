window.onload = function () {


    var
        body = $("body"),
        active = "active",
        hidden = "hidden",
        btnNav = $(".btn_navigation"),
        fildNav = $(".main_nav"),
        btnRubrics = $(".btn_rubrics"),
        fildRubrics = $(".bl_rubrics"),

        btnSearch = $(".btn_search"),
        searchFild = $(".bl_search");


    ///  sliderEffect
    function sliderEffectForButtons(button, element, duration) {
        button.click(function () {
            element.slideToggle(duration);
        });
    }


    /// toggle class Active
    function toggleClassActive(btn, fild) {
        btn.on("click", function () {
            fild.toggleClass(active);
        });
    }

    //  hideShowEffect
    function hideShowEffect(button, element) {
        button.click(function () {
            element.toggleClass("hidden");
        });
    }

    // LikeLinks buttons
    function likeLink(btn) {
        btn.on("click", function () {
            var urlLink = $(this).attr("data-href");
            if (urlLink === "") {
                alert("URL address isn't correct / шляпа");
            } else {
                window.open(urlLink);
            }
        })
    }

    likeLink($(".js-visited"));
    // likeLink($(".btn_bonus__getBonus"));


    var btnSymbols = $(".btn_moreSymbolInfo"),
        symbolsItem = $(".game_symbol__wrapper");

    sliderEffectForButtons(btnSymbols, symbolsItem, 500);


    var btnBonusInfo = $(".btn_bonus__info");

    btnBonusInfo.on("click", function () {
        $(this).parent(".bl_bonus").toggleClass("bl_bonus__active");
    });

    if (body.width() <= 991) {



        /////////// BestCasino

        //
        // $(".bl_bestCasinos__full").slick({
        //     dots: false,
        //     infinite: true,
        //
        //     autoplay: true,
        //     autoplaySpeed: 2000,
        //
        //     // centerMode: false,
        //     speed: 2000,
        //     adaptiveHeight: true,
        //     slidesToShow: 3,
        //     slidesToScroll: 1,
        //     responsive: [
        //         {
        //             breakpoint: 991,
        //             settings: {
        //                 slidesToShow: 3
        //             }
        //         },
        //         {
        //             breakpoint: 768,
        //             settings: {
        //                 slidesToShow: 2,
        //                 slidesToScroll: 2
        //             }
        //         },
        //         {
        //             breakpoint: 600,
        //             settings: {
        //                 slidesToShow: 1,
        //                 slidesToScroll: 1
        //             }
        //         },
        //         {
        //             breakpoint: 320,
        //             settings: {
        //                 slidesToShow: 1,
        //                 slidesToScroll: 1
        //             }
        //         }
        //
        //     ]
        // });


    }


    if (body.width() <= 768) {

        sliderEffectForButtons(btnNav, fildNav, 500);
        sliderEffectForButtons(btnRubrics, fildRubrics, 500);


        btnSearch.on("click", function () {
            fildNav.removeClass(active);
            searchFild.toggleClass(active);
        });


    }
    //// Progress Bar
    var progressBar = $(".js-progress-bar"),
        percentageComplete = $(".bl_ratingCasino__number").text();

    var strokeDashOffsetValue = 100 - (percentageComplete * 20);

    progressBar.css("stroke-dashoffset", strokeDashOffsetValue);


    ////////// Show Reviews Casino
    var btnShowReviews = $(".btn_leaveFeedback"),
        btn_leaveFeedback__text = $(".btn_leaveFeedback__text"),
        counterShowReviews = 0;

    btnShowReviews.on("click", function () {
        counterShowReviews++;
        btn_leaveFeedback__text.toggleClass(hidden);

        if (counterShowReviews % 2 !== 0) {
            $(".bl_reviews").removeClass(hidden);
            $(".bl_ceo__casino").addClass(hidden);
        } else {
            $(".bl_reviews").addClass(hidden);
            $(".bl_ceo__casino").removeClass(hidden);
        }

    });


    //// Design slider

    var designSlide = $(".slider_casinoDesign__item").length;
    if (designSlide > 3) {

        $('.slider_casinoDesign').slick({
            dots: false,
            infinite: true,

            // autoplay: true,
            autoplaySpeed: 2000,

            // centerMode: true,
            speed: 1500,
            slidesToShow: 3,
            slidesToScroll: 1,
            responsive: [
                {
                    breakpoint: 991,
                    settings: {
                        slidesToShow: 3,
                        centerMode: false

                    }
                },
                {
                    breakpoint: 600,
                    settings: {
                        slidesToShow: 2,
                        centerMode: false,
                        slidesToScroll: 1
                    }
                }

            ]
        });
    }


    ////////// Looking for cursor
    if (body.width() >= 1200) {

        var leftEye = $("#leftEye");
        var rightEye = $("#rightEye");

        var doc = {
            w: $(window).width(),
            h: $(window).height(),
            wC: $(window).width() / 2,
            hC: $(window).height() / 2
        };
        var mouse = {x: 0, y: 0};

        $(window).resize(function () {
            doc = {
                w: $(window).width(),
                h: $(window).height(),
                wC: $(window).width() / 2,
                hC: $(window).height() / 2
            }
        });

        $(window).mousemove(function moveObject(e) {
            mouse.x = ((e.pageX - doc.w + doc.wC) / (doc.wC / 10));
            mouse.y = ((e.pageY - doc.h + doc.hC) / (doc.hC / 10));


            leftEye.css('transform', 'translate(' + (mouse.x * 0.3) + 'px, ' + (mouse.y * 0.3) + 'px)');
            rightEye.css('transform', 'translate(' + (mouse.x * 0.3) + 'px, ' + (mouse.y * 0.3) + 'px)');
        });

        ///////////// Scrollbar

    }

    // Ancor to top

    $(".bl_ancor").on("click", "a", function (event) {
        event.preventDefault();
        var id = $(this).attr('href'),
            top = $(id).offset().top;
        $('body,html').animate({scrollTop: top}, 500);
    });
    $(window).scroll(function () {


        var ancor = $(".bl_ancor"),
            windowHeight = $(window).height() / 2;

        if ($(this).scrollTop() > windowHeight) {

            ancor.removeClass("hidden");

        } else {
            ancor.addClass("hidden");
        }

    });


};


